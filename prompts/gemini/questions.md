Generate {{count}} abstract, mystical questions for a tarot reading with intention: "{{intention}}".

Use these categories: {{categories}}.

Avoid generating questions similar to these existing questions: {{existingQuestions}}

Return only valid JSON array of strings, no markdown.
